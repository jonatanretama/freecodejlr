function findElement(arr, func) {
  let num=0;  
  for(let i in arr){
    num = arr[i];
    if(func(num)){
      return num;
    }
  }
  return undefined;
  
}

findElement([1, 3, 5, 8, 9, 10], num => num % 2 === 0);
